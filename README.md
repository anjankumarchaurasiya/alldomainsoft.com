npm install 

#connect db 
replace db credentials

#call api
1) get api http://localhost:4000/populate
2) post api http://localhost:4000/search
{
    "name": "name101501",
    "limit": 10,
    "sort": "createdAt"
} 
